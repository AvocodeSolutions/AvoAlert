{
  "name": "avoalert-monorepo",
  "version": "1.0.0",
  "description": "Real-time crypto signal platform with UT Bot indicator",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/backend/*",
    "apps/frontend/web-client"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^1.10.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}